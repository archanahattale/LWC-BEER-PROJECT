import { LightningElement, track } from 'lwc';

export default class Samplecomponent extends LightningElement {
    @track greeting="Hello Archana";
    greetuser(event){
        let txtinput=this.querySelector
    }
}